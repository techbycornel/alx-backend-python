#!/bin/bash

# Scale replicas to 3
echo "📈 Scaling Django app to 3 replicas..."
kubectl scale deployment messaging-app-deployment --replicas=3

# Verify
kubectl get pods

# Optional: Load test using wrk
# wrk -t12 -c400 -d30s http://<service-cluster-ip>:8000/

# Monitor resource usage
kubectl top pods
